diff --git a/main.go b/main.go
--- a/main.go
+++ b/main.go
@@ -58,7 +58,7 @@ func main() {
 	Main()
 }
 
-const version = "local-build"
+const version = "1.3.2-mobius.1"
 
 // cliUsage returns the docopt usage string that drives command dispatch.
 // It is a function (not inlined in Main) so tests can parse real command
@@ -1454,16 +1454,19 @@ func outputDetailedList(deviceList ios.DeviceList) {
 func outputDetailedList(deviceList ios.DeviceList) {
 	result := make([]detailsEntry, len(deviceList.DeviceList))
 	for i, device := range deviceList.DeviceList {
 		udid := device.Properties.SerialNumber
+		result[i] = detailsEntry{
+			Udid:           udid,
+			ConnectionType: device.ConnectionTypeLabel(),
+		}
 		allValues, err := ios.GetValues(device)
-		exitIfError("failed getting values", err)
-		result[i] = detailsEntry{
-			Udid:           udid,
-			ProductName:    allValues.Value.ProductName,
-			ProductType:    allValues.Value.ProductType,
-			ProductVersion: allValues.Value.ProductVersion,
-			ConnectionType: device.ConnectionTypeLabel(),
+		if err != nil {
+			slog.Warn("failed getting values for device list entry", "udid", udid, "error", err)
+			continue
 		}
+		result[i].ProductName = allValues.Value.ProductName
+		result[i].ProductType = allValues.Value.ProductType
+		result[i].ProductVersion = allValues.Value.ProductVersion
 	}
 	fmt.Println(convertToJSONString(map[string][]detailsEntry{
 		"deviceList": result,
diff --git a/ios/forward/forward.go b/ios/forward/forward.go
--- a/ios/forward/forward.go
+++ b/ios/forward/forward.go
@@ -34,7 +34,7 @@ func Forward(device ios.DeviceEntry, hostPort uint16, phonePort uint16) (*ConnLis
 		return nil, fmt.Errorf("forward: invalid target port: port must be at least 1")
 	}
 	golog.Info("start listening, forwarding to device", "module", logModule, "udid", device.Properties.SerialNumber, "hostPort", hostPort, "phonePort", phonePort)
-	l, err := net.Listen("tcp", fmt.Sprintf("0.0.0.0:%d", hostPort))
+	l, err := net.Listen("tcp", fmt.Sprintf("127.0.0.1:%d", hostPort))
 	if err != nil {
 		return nil, fmt.Errorf("forward: failed listener with err: %w", err)
 	}
diff --git a/ios/instruments/screenshot.go b/ios/instruments/screenshot.go
--- a/ios/instruments/screenshot.go
+++ b/ios/instruments/screenshot.go
@@ -67,8 +67,8 @@ func startServer(device ios.DeviceEntry, port string) error {
 	go startScreenshotting(conn)
 	go startConversionQueue()
 	http.HandleFunc("/", mjpegHandler)
-	location := fmt.Sprintf("0.0.0.0:%s", port)
-	golog.Info("starting server, open your browser here", "module", logModule, "udid", device.Properties.SerialNumber, "host", "0.0.0.0", "port", port, "url", fmt.Sprintf("http://%s/", location))
+	location := fmt.Sprintf("127.0.0.1:%s", port)
+	golog.Info("starting server, open your browser here", "module", logModule, "udid", device.Properties.SerialNumber, "host", "127.0.0.1", "port", port, "url", fmt.Sprintf("http://%s/", location))
 	return http.ListenAndServe(location, nil)
 }
 
